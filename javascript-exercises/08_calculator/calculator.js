const add = function(...arg) {
  let total = 0;
  // arg.forEach(number => total += number);
  for (let number of arg) {
    total += number;
  }
  return total;
};

const subtract = function() {
	
};

const sum = function() {
	
};

const multiply = function() {

};

const power = function() {
	
};

const factorial = function() {
	
};

// Do not edit below this line
module.exports = {
  add,
  subtract,
  sum,
  multiply,
  power,
  factorial
};
